<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:configChanges="orientation|screenSize|keyboardHidden"
        android:fullBackupOnly="true"
        android:icon="@mipmap/badgr_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/badgr_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BadgR">
        <activity
            android:name=".scoutPagesAndClasses.scoutPage"
            android:exported="false"
            android:label="@string/title_activity_scout_tab"
            android:theme="@style/Theme.BadgR.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true"></activity>
        <activity android:name=".ui.register.RegisterActivity" />
        <activity android:name="Fragments.SMFrags.SMRecentAllNots" />
        <activity
            android:name=".scoutPagesAndClasses.scoutMasterPage"
            android:label="@string/title_activity_scout_tab"
            android:theme="@style/Theme.BadgR.NoActionBar" />
        <activity android:name="Fragments.SMFrags.SMScoutProfile" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>